{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Title-bar dragging and the updater — desktop only",
  "platforms": ["linux", "macOS", "windows"],
  "windows": ["main"],
  "permissions": [
    "core:window:allow-start-dragging",
    "updater:default",
    "process:default"
  ]
}
