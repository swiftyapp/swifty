.DS_Store
.env
.env.local

# JS / Vite
node_modules
dist
dist-ssr
*.local

# Rust / Tauri
src-tauri/target
src-tauri/gen/schemas

# Updater signing keys — never commit
*.key
*.key.pub

# Claude Code local state & agent worktrees
.claude/
