.DS_Store
.env
.env.local

# JS / Vite
node_modules
dist
dist-ssr
*.local

# Rust / Tauri
src-tauri/target
src-tauri/gen/schemas

# Updater signing keys — never commit
*.key
*.key.pub

# Claude Code local state & agent worktrees
.claude/

# Local dev vault used when running the app from the workspace (see SWIFTY_DB_DIR)
.dev-vault/
