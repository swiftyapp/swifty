// Windows custom title-bar controls. Window chrome is owned by PR-5, so this
// renders nothing for now (parity: legacy also rendered null on non-Windows).
export default function Controls() {
  return null
}
